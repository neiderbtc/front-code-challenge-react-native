import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    SubmitButtonStyle: {
        marginTop: 10,
        paddingTop: 20,
        padding: 20,
        width: "100%",
        paddingBottom: 20,
        backgroundColor: '#273c75',
        borderRadius: 10,
        borderWidth: 1,
        borderColor: '#fff'
    },
    TextStyle: {
        color: '#fff',
        textAlign: 'center',
    },

})